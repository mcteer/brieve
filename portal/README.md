# Portal

**Reserved stub.** Conversational web UI (TypeScript) will live here. The
Node/TypeScript toolchain is **out of scope** for feature 001 — do not add
`package.json` or related tooling until a later feature establishes it.

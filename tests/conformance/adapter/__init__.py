# SPDX-License-Identifier: Apache-2.0
"""Primary-adapter conformance lane.

In force for 004 (ADR-0047, constitution v1.0.1): governance-first ordering,
fail-closed denial, and governed-entry (``invoke_tool``) interception. Rows for
features that have not shipped are absent or carry a single explicit skip naming
the ADR that defers them — never a passing stub. See
``specs/004-primary-adapter/contracts/conformance-adapter.md``.
"""

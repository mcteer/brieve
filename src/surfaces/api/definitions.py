# SPDX-License-Identifier: Apache-2.0
"""Agent definition enumeration — the public view (011 US5)."""

from __future__ import annotations

from fastapi import APIRouter


def build_router() -> APIRouter:
    """Placeholder until US5; the assembly references it so wiring lands once."""
    return APIRouter()
